from notices.models import *
from notices.constants import *

for i in DEPARTMENTS:
  c = Category(name=i[1], main_category="Departments", code=i[0])
  c.save()

for i in AUTHORITIES:
  c = Category(name=i[1], main_category="Authorities", code=i[0])
  c.save()
